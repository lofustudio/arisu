{
  "name": "bot",
  "version": "1.0.0",
  "description": "",
  "license": "MIT",
  "author": {
    "name": "lofustudio",
    "email": "hello@lofu.studio"
  },
  "main": "build/index.js",
  "scripts": {
    "dev": "tsnd --respawn --transpile-only --poll .",
    "build": "rimraf ./build && tsc -p .",
    "start": "node ./build/index.js",
    "clean": "rimraf dist .turbo",
    "clear": "pnpm run clean && rimraf node_modules"
  },
  "dependencies": {
    "discord.js": "^14.8.0"
  },
  "devDependencies": {
    "@arisu/db": "workspace:*",
    "@arisu/tsconfig": "workspace:*",
    "@types/node": "^17.0.12",
    "ts-node-dev": "^2.0.0",
    "typescript": "^4.5.3"
  }
}